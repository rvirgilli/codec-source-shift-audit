# Superseded provenance note

The earlier version of this file overstated which analyses had a public pre-run
record. See `ANALYSIS_PROVENANCE.md` for the Git-backed chronology.

Only the uniform nine-fold MLAAD extension has a rule commit in this repository that
predates its output commit (`0d1b7d0` before `f419e53`). The targeted TORTOISE run and
the ASVspoof 5 sweep predate the first public rule file and are retrospectively
documented. The crossed, multiplicity-adjusted, simultaneous-rank, and tie-correct
submission analyses are revision-stage robustness analyses and were not preregistered.
